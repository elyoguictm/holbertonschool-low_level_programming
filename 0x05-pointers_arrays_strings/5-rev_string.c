#include "holberton.h"

/**
 * rev_string - rev string
 * @s: ssssss
 */

void rev_string(char *s)
{
	int len, i;
	char c;

	for (len = 0; s[len] != 0; len++)
	{
	}
	for (len = len - 1; len >= 0; len--)
	{

	}
}
